import "./styles.css";
console.log("hello odin enjoyers hehe <3");

// use this to initialize n place all ships

// ship.forEach(element => {

//     const placementType = this.horizontalOrVerticalShipPlacement();
//         const validCoord = this.calculateValidCoordinates(ship,placementType);
// placeShips(ship.length,placementType,validCoord)

// });
